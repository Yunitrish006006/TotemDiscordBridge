{
  "schemaVersion": 1,
  "id": "totem-discord-bridge",
  "version": "${version}",
  "name": "TotemDiscordBridge",
  "icon": "assets/totem-discord-bridge/icon.png",
  "environment": "*",
  "entrypoints": {
    "main": ["dev.totem.discord.TotemDiscordBridge"],
    "client": ["dev.totem.discord.TotemDiscordBridgeClient"]
  },
  "mixins": [
    "totem-discord-bridge.mixins.json",
    { "config": "totem-discord-bridge.client.mixins.json", "environment": "client" }
  ],
  "depends": { "fabricloader": ">=0.19.3", "fabric-api": "*", "minecraft": "~26.2", "java": ">=25", "totem-core": "=0.4.0" }
}
